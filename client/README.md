# Client (React)

This folder contains the React application (Create React App).

## Setup
```bash
cd client
npm install
```

## Environment
Copy `.env.example` to `.env.local` and update `REACT_APP_API_URL` if your API is hosted elsewhere.

## Run
```bash
npm start
```
